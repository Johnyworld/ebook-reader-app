## 테스트 방침

- 순수 로직(파서, 헬퍼 함수, 마이그레이션 등)은 가능하면 TDD로 진행한다. 테스트를 먼저 작성하고, 테스트를 통과하도록 구현한다.
- UI 코드(Compose, WebView 연동 등)는 구현 후 필요시 테스트를 추가한다.

## e-ink 기기 최적화 룰

- 애니메이션은 구현하지 않는다.
- 회색 배경색은 사용하지 않는다. 배경색은 무조건 흰색만 사용한다.
- 글자 색상을 사용하지 않는다. 대부분 검정색이고, 필요 시 회색을 사용할 수 있다.